import { ICourse } from "../../models/course"
import { IProgram } from "../../models/program"

const advancedSearch = (courses: ICourse[], program: IProgram) => {
    return {}
}

export default advancedSearch
